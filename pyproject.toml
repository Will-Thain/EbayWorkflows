[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ebay-workflows"
version = "0.1.0"
description = "Local CLI workflows for MTG eBay listing analysis."
readme = "docs/README.md"
requires-python = ">=3.11"
dependencies = [
  "alembic",
  "httpx",
  "mtg-card-recognition @ git+https://github.com/Will-Thain/mtg-card-recognition.git@eede35b",
  "paddleocr",
  "psycopg[binary]",
  "pydantic>=2",
  "pydantic-settings",
  "rapidfuzz",
  "rich",
  "sqlalchemy",
  "structlog",
  "tenacity",
  "typer",
]

[project.optional-dependencies]
recognition = []
gui = [
  "pyside6>=6.6",
]
dev = [
  "pytest",
  "ruff",
]
gpu = [
  "torch-directml",
]

[project.scripts]
ebay-workflows = "ebay_workflows.cli:app"
ebay-workflows-gui = "ebay_workflows.gui.qt_app:main"
ebay-workflows-run-due-schedules = "ebay_workflows.scheduler:run_due_schedules_main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["ebay_workflows*"]
